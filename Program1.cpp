#include <iostream>
#include "ImageLib.h"
#include <string>

//string for filename
const std::string fileName = "test1.gif";

int main(){
    image input = READGIF(fileName);
    //check if image is valid 
    if
    int test = test;
    std::
}